# Troubleshooting

Common issues:

- `401 Unauthorized`: verify JWT or API key scope.
- `429 Rate limited`: slow campaign cadence or rotate proxies.
- Empty dataset export: confirm the campaign has a completed run with a dataset.
- LinkedIn login wall: refresh `li_at` session cookie.
